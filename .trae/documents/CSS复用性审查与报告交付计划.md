## 目标
- 对 src/pages 与 src/styles 的 HTML/CSS/模板字符串进行全面审查，识别可复用样式组合、重复定义与相似命名，生成带来源与行号的 Markdown 报告。

## 范围与对象
- HTML: src/pages/help/help.html 及各页面 JS 中的内嵌模板HTML（Calc/History/Config/Query/Popup）。
- CSS: src/styles/main.css、src/pages/help/help.css；同时标记 JS 内联 style 属性与在 JS 中动态注入的样式片段。

## 方法
1. 枚举并解析所有类与ID：class/id/内联style，以及 JS 模板中的表格与布局结构。
2. 合并同类项：将相同或近似视觉表现的样式属性组合归类。
3. 以候选形式输出：为每类样式给出“可抽象的通用类”建议、来源文件与行号、重复/相似评分与复用场景。
4. 统计收益：估算复用后可减少的行数/字节数，并说明维护收益。

## 交付内容
- Markdown 报告（执行摘要、详细分析数据（含表格）、结论建议）。
- 所有来源以可点击的文件路径+行号链接呈现，便于代码定位与后续重构。

## 后续可选改进（待确认后执行）
- 在 src/styles 下新增 utilities.css，沉淀通用容器、按钮、输入、表格、栅格/间距工具类。
- 将页面内联 style 与重复样式迁移为通用类，并删除冗余定义；确保不改变现有视觉表现。
- 为多选组件（multi-select）与仿真区 legend 抽象成组件级样式，统一跨页面行为与皮肤。

## 风险与边界
- 保持当前文件不变，仅输出分析；任何样式合并需要迭代验证以避免特异性与覆盖问题。

请确认以上计划后，我将按该方案输出并迭代完善报告（本次已生成首版报告）。